# Minimal RAG Pipeline Requirements
# Only essential dependencies for fast installation

# 🔹 PDF processing
PyMuPDF>=1.23.0

# 🔹 LangChain framework (minimal set)
langchain>=0.1.0
langchain-community
langchain-google-genai>=1.0.0

# 🔹 Embeddings
sentence-transformers>=2.2.0

# 🔹 Vector database
chromadb>=0.4.0

# 🔹 Google AI integration
google-generativeai>=0.3.0

# 🔹 Environment variables
python-dotenv>=1.0.0

# 🔹 FastAPI backend
fastapi>=0.104.0
uvicorn[standard]>=0.24.0

# 🔹 User Authentication
pymongo>=4.0.0
motor>=3.0.0
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4
python-multipart>=0.0.6
email-validator>=2.0.0

# Built-in Python modules used (no installation needed):
# - os, io, uuid, typing (built-in)
# - jupyter (if already installed)

